{
  "name": "local-debugger",
  "version": "1.0.0",
  "description": "This is a sample debugger for Typescript, based on ask-sdk-local-debug logic.",
  "main": "index.js",
  "type": "commonjs",
  "scripts": {
    "clean": "rimraf dist/debugger",
    "compile": "pwd && npm run clean && cd .. && pwd && tsc -b tsconfig.json && echo \"finish build\" && cd local && pwd && cp package.json ./my-dist/package.json && npm i && cd .. && pwd"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "ask-sdk-core": "^2.12.1",
    "ask-sdk-model": "^1.40.0",
    "express": "^4.18.2",
    "ws": "^8.11.0"
  },
  "devDependencies": {
    "@types/node": "^18.11.18",
    "@types/ws": "^8.5.4",
    "ask-sdk-local-debug": "^1.1.0",
    "rimraf": "^3.0.2"
  }
}